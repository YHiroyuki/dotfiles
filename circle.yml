machine:
  timezone:
    Asia/Tokyo
  environment:
    SAMPLE: sample
    TEST_BRANCH: my-mac-settings

test:
  override:
    - test $SAMPLE

deployment:

  env_branch_name:
    branch: "$TEST_BRANCH"
    commands:
      - echo $SAMPLE
  hoge:
    branch: my-mac-settings
    commands:
      - echo "XXX"
